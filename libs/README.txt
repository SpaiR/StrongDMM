All (for all platforms) native libraries needed to ru application.

Most interesting of it is 'sdmmparser' which is actually a small library which provides parsed '.dme' objects.
Library is using parser called SpacemanDMM. Parser itself could be found by next link: https://github.com/SpaceManiac/SpacemanDMM
